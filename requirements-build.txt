# Build dependencies for markitdown binaries
# This file includes PyInstaller and all dependencies needed for a full-featured build

# PyInstaller for building binaries
pyinstaller>=6.0.0

# Core dependencies (from markitdown base requirements)
beautifulsoup4
requests
markdownify
magika~=0.6.1
charset-normalizer
defusedxml

# Platform-specific: onnxruntime for Windows (PyInstaller will bundle it appropriately)
onnxruntime<=1.20.1; sys_platform == 'win32'

# Optional dependencies for full functionality (all extras)
python-pptx
mammoth~=1.10.0
pandas
openpyxl
xlrd
lxml
pdfminer.six
olefile
pydub
SpeechRecognition
youtube-transcript-api~=1.0.0
azure-ai-documentintelligence
azure-identity
